version: '3'

services:

  postgres:
    environment:
      POSTGRES_USER: "${POSTGRES_USER}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
      DB_PASS: "${DB_PASS}"
      DB_USER: "${DB_USER}"
      DB_API: "${DB_API}"
      DB_CORE: "${DB_CORE}"
    ports:
      - "5432:5432"